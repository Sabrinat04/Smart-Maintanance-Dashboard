import { AppShell } from '@/components/AppShell'
import { DataTable, PageHeader, StatusBadge, card } from '@/components/ModuleUI'
export default function Page(){const rows=[['WO-8294','Before: available','After: available','Replaced broken window handle','2h 15m','On Track'],['WO-8302','Before: missing','After: available','Need proof of leak source fix','1h 40m','Photo Missing']]
return <AppShell role='pm' title='Completed Work Verification'><PageHeader title='Completed Work Verification'/><DataTable headers={['Request','Before Photo','After Photo','Technician Notes','Work Duration','SLA Result','Actions']} rows={rows.map(r=>[r[0],r[1],r[2],r[3],r[4],<StatusBadge key='s' value={r[5]}/>,<div key='a' className='flex gap-2'><button className='text-emerald-700'>Verify & Close</button><button className='text-red-700'>Reject Verification</button><button>Request More Info</button></div>])}/><div className={`${card} mt-4 text-amber-700`}>Missing photo warning: WO-8302 cannot be verified until before-photo is uploaded.</div></AppShell>}
